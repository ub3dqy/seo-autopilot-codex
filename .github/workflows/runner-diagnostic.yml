name: Runner diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner
        run: echo "GitHub-hosted runner is available"
