# v1.5.3 field acceptance

The release is acceptable only when all conditions below hold for the exact candidate and the exact merged runtime commit.

```text
runtime version = 1.5.3
SOURCE_FIRST scope present = true
active Next.js cookies route scanned = true
active history/preferences/bookmarks routes scanned = true
source paths classified EXCLUDED_SENSITIVE = 0
actual browser profile classified EXCLUDED_SENSITIVE = true
browser profile files_not_read = true
findings from artifacts/** = 0
findings from tmp/** = 0
A-level fixes outside CURRENT_SOURCE = 0
repeat audit deterministic = true
full local release gate = PASS
direct User ZIP audit/verify = PASS
direct Engineering ZIP audit/verify = PASS
release asset round-trip SHA-256 = PASS
one-link bootstrap pins exact v1.5.3 commit/tree/asset hash = true
```

The field regression must reproduce the route shape reported by AIRSYS:

```text
src/app/(site)/(legacy)/cookies/page.tsx
```

A directory name alone is never browser database evidence. Privacy classification requires regular-file metadata plus corroborating browser context. No marker content may be read.

```text
GitHub Actions: BLOCKED_EXTERNAL / WAIVED_BY_OWNER
Release gate: LOCAL VERIFICATION ONLY
```
