# v1.5.2 local verification

The exact public GitHub branch archive for `feat/v1.5.2-audit-scope-privacy` was downloaded and verified locally without GitHub Actions.

Mandatory command:

```bash
python scripts/verify_local.py --release
```

Result: `PASS`.

Verified areas include:

- all existing unit, lifecycle, transaction, rollback, installer, reporting, release, and source-version tests;
- AIRSYS-shaped source-scope regression with 150 archived HTML pages, 50 temporary HTML pages, and browser-profile markers;
- zero findings and zero A-level candidates from `artifacts/**` and `tmp/**`;
- `EXCLUDED_SENSITIVE` browser-profile recording with `files_not_read=true`;
- deterministic Next.js source findings for hash navigation, mobile menu accessibility, sitemap dates, WebSite identity, and dynamic route metadata ownership;
- source verification and tracked-source secret scan;
- two deterministic edition builds and reproducibility comparison;
- direct-from-ZIP version checks;
- SPDX SBOM, release asset verification, and post-build generated-tree verification.

```text
GitHub Actions: BLOCKED_EXTERNAL / WAIVED_BY_OWNER
Release gate: LOCAL VERIFICATION ONLY
```
